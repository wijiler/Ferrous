
pub enum Tokens {
    Strings(String),
    Operator(char),
    Int(i64),
    Uint(u64),
    Float(f64),
    Lparen,
    Rparen,
    Semi,
    Rbrack,
    Lbrack,
    RCbrack,
    LCbrack,
}

pub fn main () { }

